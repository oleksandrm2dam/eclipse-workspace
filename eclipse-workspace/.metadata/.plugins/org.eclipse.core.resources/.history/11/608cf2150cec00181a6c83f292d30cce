package notaMedia;

import java.io.File;
import java.io.FileReader;

public class ContadorNotas {
	
	private File ficheroConNotas;
	private static final char SEPARADOR = '-'; // Este caracter se usa para separar cada nota en el fichero
	// Ejemplo: 8-7-3-9-10-4-4-3
	
	public ContadorNotas(File ficheroConNotas) {
		this.ficheroConNotas = ficheroConNotas;
	}

	public static void main(String[] args) {
		ContadorNotas cn = new ContadorNotas(new File(args[0]));
		System.out.println(cn.contarMedia());
	}
	
	public double contarMedia() {
		double total = 0;
		double cantidad = 0;
		FileReader fr = null;
		int i;
		String tempNum = "";
		
		try {
			// Abro flujo de lectura y recorro el fichero entero
			fr = new FileReader(ficheroConNotas);
			while((i = fr.read()) != -1) {
				if((char) i == SEPARADOR) {
					
				} else {
					tempNum += (char) i;
				}
			}
		} catch (Exception e) {
			e.printStackTrace();
		} finally {
			// Cierro el flujo de lectura al fichero
			try {
				if(fr != null) {
					fr.close();
				}
			} catch (Exception e) {
				e.printStackTrace();
			}
		}
		
		return total / cantidad;
	}	
	
}
